export interface TemplateRouteProps {
    title: string;
    details?: string;
    textButton?: string;
    image: string;
    email: string;
    github: string;
    children?: React.ReactNode;
    link: string;
}