export interface ScrollSnapProps {
    main: JSX.Element;
    work: JSX.Element;
    about: JSX.Element;
    contact: JSX.Element;
}
export interface SectionScrollSnapProps {
    component: React.ReactNode;
    id: string;
}