export interface SidebarStyleProps {
    isSideBarVisible?: boolean;
    isMobile?: boolean;
}

export interface SidebarProps {
    linkedin: string;
    github: string;
    whatsapp?: string;
    instagram: string;
}
